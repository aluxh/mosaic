{
  "name": "mosaic",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted Shared Memory Wall for memorials and celebrations",
  "scripts": {
    "install:all": "npm install && npm --prefix web install && npm --prefix api install",
    "dev": "concurrently -n web,api -c cyan,magenta \"npm:dev:web\" \"npm:dev:api\"",
    "dev:web": "npm --prefix web run dev",
    "dev:api": "npm --prefix api run dev",
    "build": "npm --prefix web run build && npm --prefix api run build",
    "test": "npm --prefix web run test -- --run && npm --prefix api run test -- --run",
    "test:web": "npm --prefix web run test",
    "test:api": "npm --prefix api run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
